<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Reuse Odoo's existing Product group-by filter and retain its standard
         To Do filter and default company. Users can remove the grouping. -->
    <record id="mrp.mrp_production_action" model="ir.actions.act_window">
        <field name="context">{'search_default_todo': True, 'default_company_id': allowed_company_ids[0], 'search_default_product': 1}</field>
    </record>
</odoo>
